import { Meta, Story, Canvas } from "@storybook/addon-docs";
import SVG from "react-inlinesvg";

import Button from "./Button";
import { Card, CardBody } from "../Card";

import searchIcon from "../../images/icons/search.svg";
import userIcon from "../../images/icons/star.svg";

<Meta
  title="Molecules/Button"
  component={Button}
  parameters={{
    design: {
      type: "figma",
      url: "https://www.figma.com/file/fo4DadnYGfvtWawiQYcwmj/HDR-UK---Library?node-id=1021%3A16226",
    },
  }}
/>

<Canvas>
  <Story name="Variants">
    {(props) => (
      <Card>
        <CardBody>
          <div>
            <Button {...props} mb={4} mr={2}>
              Primary
            </Button>
            <Button {...props} mb={4} disabled>
              Primary (disabled)
            </Button>
          </div>
          <div>
            <Button {...props} variant="secondary" mb={4} mr={2}>
              Secondary
            </Button>
            <Button {...props} variant="secondary" mb={4} disabled>
              Secondary (disabled)
            </Button>
          </div>
          <div>
            <Button {...props} variant="tertiary" mr={2}>
              Tertiary
            </Button>
            <Button {...props} variant="tertiary" disabled>
              Tertiary (disabled)
            </Button>
          </div>
        </CardBody>
      </Card>
    )}
  </Story>
  <Story name="Sizes">
    {(props) => (
      <Card>
        <CardBody>
          <div>
            <Button {...props} mb={4} size="large">
              Large
            </Button>
          </div>
          <div>
            <Button {...props} mb={4}>
              Medium
            </Button>
          </div>
          <div>
            <Button {...props} size="small">
              Small
            </Button>
          </div>
        </CardBody>
      </Card>
    )}
  </Story>
  <Story name="Icons">
    {(props) => (
      <Card>
        <CardBody>
          <div>
            <Button {...props} mb={4} iconLeft={<SVG src={searchIcon} />}>
              Icon left
            </Button>
          </div>
          <div>
            <Button
              {...props}
              mb={4}
              iconRight={<SVG src={userIcon} fill="white" />}
            >
              Icon right
            </Button>
          </div>
          <div>
            <Button
              {...props}
              iconLeft={<SVG src={searchIcon} />}
              iconRight={<SVG src={userIcon} />}
            >
              Icon left and right
            </Button>
          </div>
        </CardBody>
      </Card>
    )}
  </Story>
</Canvas>
