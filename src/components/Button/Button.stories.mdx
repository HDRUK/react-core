import { Meta, Story, Canvas } from "@storybook/addon-docs";
import SVG from "react-inlinesvg";
import tickIcon from "../../images/icons/search.svg";
import userIcon from "../../images/icons/star.svg";
import Button from "./Button";

<Meta title="Core/Button" component={Button} />

<Canvas>
  <Story name="Variants">
    {(props) => (
      <div>
        <div>
          <Button {...props} mb={4} mr={2}>
            Primary
          </Button>
          <Button {...props} mb={4} disabled>
            Primary (disabled)
          </Button>
        </div>
        <div>
          <Button {...props} variant="secondary" mb={4} mr={2}>
            Secondary
          </Button>
          <Button {...props} variant="secondary" mb={4} disabled>
            Secondary (disabled)
          </Button>
        </div>
        <div>
          <Button {...props} variant="tertiary" mb={4} mr={2}>
            Tertiary
          </Button>
          <Button {...props} variant="tertiary" mb={4} disabled>
            Tertiary (disabled)
          </Button>
        </div>
      </div>
    )}
  </Story>
  <Story name="Sizes">
    {(props) => (
      <div>
        <div>
          <Button {...props} mb={4} size="large">
            Large
          </Button>
        </div>
        <div>
          <Button {...props} mb={4}>
            Medium
          </Button>
        </div>
        <div>
          <Button {...props} mb={4} size="small">
            Small
          </Button>
        </div>
      </div>
    )}
  </Story>
  <Story name="Icons">
    {(props) => (
      <div>
        <div>
          <Button
            {...props}
            mb={4}
            iconLeft={<SVG src={userIcon} stroke="white" />}
          >
            Icon left
          </Button>
        </div>
        <div>
          <Button
            {...props}
            mb={4}
            iconRight={<SVG src={userIcon} stroke="white" />}
          >
            Icon right
          </Button>
        </div>
        <div>
          <Button
            {...props}
            mb={4}
            iconLeft={<SVG src={tickIcon} stroke="white" />}
            iconRight={<SVG src={userIcon} stroke="white" />}
          >
            Icon left and right
          </Button>
        </div>
      </div>
    )}
  </Story>
</Canvas>
