import { useState } from "react";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";

import Dimmer from "./Dimmer";
import { Card, CardBody } from "../Card";

<Meta title="Atoms/Dimmer" component={Dimmer} />

<Canvas>
  <Story name="Basic">
    {(props) => {
      return (
        <div>
          <Card>
            <CardBody>Content behind</CardBody>
          </Card>
          <Dimmer {...props}>Dimmer content</Dimmer>
        </div>
      );
    }}
  </Story>
</Canvas>
