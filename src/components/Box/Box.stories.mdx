import { useState } from "react";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";

import Box from "./Box";

<Meta title="Atoms/Box" component={Box} />

<Canvas>
    <Story name="Responsive">
        {props => {
            return (
                <Card>
                    <CardBody>
                        <Box
                            display="flex"
                            flexDirection={{
                                xxs: "column",
                                md: "row",
                            }}
                            gap={{
                                xxs: 2,
                                md: 4,
                            }}
                            width="100%">
                            <Box flexGrow="1" backgroundColor="grey400" p={4}>
                                Left column
                            </Box>
                            <Box flexGrow="1" backgroundColor="grey400" p={4}>
                                Right column
                            </Box>
                        </Box>
                    </CardBody>
                </Card>
            );
        }}
    </Story>
</Canvas>

<ArgsTable of={Box} />
