import { Meta, Story, Canvas } from "@storybook/addon-docs";

import Message from "./Message";
import { Card, CardBody } from "../Card";

<Meta title="Atoms/Message" component={Message} />

<Canvas>
  <Story name="Basic">
    {(props) => (
      <Card>
        <CardBody>
          {["success", "info", "warning", "danger", "description"].map(
            (variant) => (
              <div>
                <Message variant={variant} mb={4}>
                  This is an {variant} message
                </Message>
              </div>
            )
          )}
        </CardBody>
      </Card>
    )}
  </Story>
</Canvas>
