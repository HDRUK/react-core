import { useState } from "react";
import SVG from "react-inlinesvg";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";

import Input from "./Input";
import { Card, CardBody } from "../Card";
import Icon from "../Icon";

import tickIcon from "../../images/icons/search.svg";
import userIcon from "../../images/icons/star.svg";

<Meta title="Molecules/Input" component={Input} />

<Canvas>
  <Story name="Variants">
    {(props) => {
      return (
        <Card>
          <CardBody>
            <Input {...props} mb={4} label="Primary" />
            <Input {...props} mb={4} label="Primary (disabled)" disabled />
            <Input
              {...props}
              mb={10}
              label="Primary (error)"
              error="This field is required"
            />
            <Input {...props} variant="secondary" label="Secondary" mb={4} />
            <Input
              {...props}
              variant="secondary"
              label="Secondary (disabled)"
              mb={4}
              disabled
            />
            <Input
              {...props}
              variant="secondary"
              label="Secondary (error)"
              error="This field is required"
            />
          </CardBody>
        </Card>
      );
    }}
  </Story>
</Canvas>
