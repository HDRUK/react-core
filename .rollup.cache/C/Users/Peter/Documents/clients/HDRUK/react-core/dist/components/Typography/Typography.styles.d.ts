import { Theme } from "@emotion/react";
import { TypographyStyleProps } from "./Typography.types";
export declare const root: ({ color, variant, tag }: TypographyStyleProps) => (theme: Theme) => import("@emotion/utils").SerializedStyles;
