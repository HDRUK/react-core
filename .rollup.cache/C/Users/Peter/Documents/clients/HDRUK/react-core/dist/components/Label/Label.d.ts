import { LabelProps } from "./Label.types";
declare const Label: ({ className, children, disabled, ml, mr, mb, mt, m, mx, my, width, minWidth, maxWidth, ...outerProps }: LabelProps) => import("@emotion/react/jsx-runtime").JSX.Element;
export default Label;
