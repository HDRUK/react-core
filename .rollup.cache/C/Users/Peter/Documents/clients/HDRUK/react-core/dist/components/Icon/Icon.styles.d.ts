import { Theme } from "@emotion/react";
import { IconStyleProps } from "./Icon.types";
export declare const root: ({ size, color, stroke, fill }: IconStyleProps) => (theme: Theme) => import("@emotion/utils").SerializedStyles;
