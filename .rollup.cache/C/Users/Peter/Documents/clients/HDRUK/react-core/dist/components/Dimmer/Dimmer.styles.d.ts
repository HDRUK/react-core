import { Theme } from "@emotion/react";
import { DimmerStyleProps } from "./Dimmer.types";
export declare const root: ({ contentAlignment }: DimmerStyleProps) => (theme: Theme) => import("@emotion/utils").SerializedStyles;
