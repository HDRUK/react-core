import { Theme } from "@emotion/react";
import { TagStyleProps } from "./Tag.types";
export declare const root: ({ variant, size }: TagStyleProps) => (theme: Theme) => import("@emotion/utils").SerializedStyles;
