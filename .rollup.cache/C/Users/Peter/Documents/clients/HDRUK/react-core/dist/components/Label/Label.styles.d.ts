import { Theme } from "@emotion/react";
import { LabelStyleProps } from "./Label.types";
export declare const root: ({ disabled }: LabelStyleProps) => (theme: Theme) => import("@emotion/utils").SerializedStyles;
