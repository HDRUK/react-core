import { TagProps } from "./Tag.types";
declare const Tag: {
    ({ children, mt, mb, ml, mr, m, mx, my, variant, size, className, ...outerProps }: TagProps): import("@emotion/react/jsx-runtime").JSX.Element;
    defaultProps: {
        variant: string;
        size: string;
    };
};
export default Tag;
