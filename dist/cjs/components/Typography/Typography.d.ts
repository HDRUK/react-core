import { TypographyProps } from "./Typography.types";
declare const Typography: {
    ({ children, color, className, mt, mb, ml, mr, m, mx, my, width, minWidth, maxWidth, variant, as, }: TypographyProps): import("@emotion/react/jsx-runtime").JSX.Element | null;
    defaultProps: {
        variant: string;
        m: number;
    };
};
export default Typography;
