import { Theme } from "@emotion/react";
import { IconButtonStyleProps } from "./IconButton.types";
export declare const root: () => () => import("@emotion/utils").SerializedStyles;
export declare const icon: ({ variant, size }: IconButtonStyleProps) => (theme: Theme) => import("@emotion/utils").SerializedStyles;
