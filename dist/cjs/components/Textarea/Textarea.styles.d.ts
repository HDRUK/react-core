import { Theme } from "@emotion/react";
import { TextAreaStyleProps } from "./TextArea.types";
export declare const root: ({ variant, minHeight, maxHeight, error }: TextAreaStyleProps) => (theme: Theme) => import("@emotion/utils").SerializedStyles;
