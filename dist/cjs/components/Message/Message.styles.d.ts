import { Theme } from "@emotion/react";
import { MessageStyleProps } from "./Message.types";
export declare const root: ({ variant }: MessageStyleProps) => (theme: Theme) => import("@emotion/utils").SerializedStyles;
