import { CtaProps } from "./Cta.types";
declare const Cta: {
    ({ color, children, mt, mb, ml, mr, m, mx, my, iconLeft, iconRight, className, fill, size, ...outerProps }: CtaProps): import("@emotion/react/jsx-runtime").JSX.Element;
    defaultProps: {
        size: string;
    };
};
export default Cta;
