import { CardProps } from "./Card.types";
declare const Card: ({ children, mt, mb, ml, mr, m, mx, my, width, minWidth, maxWidth, height, minHeight, maxHeight, className, ...outerProps }: CardProps) => import("@emotion/react/jsx-runtime").JSX.Element;
export default Card;
