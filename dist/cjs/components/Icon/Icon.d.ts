import { IconProps } from "./Icon.types";
declare const Icon: {
    ({ svg, size, color, fill, stroke, className, ml, mr, mb, mt, m, mx, my, ...outerProps }: IconProps): import("@emotion/react/jsx-runtime").JSX.Element;
    defaultProps: {
        size: string;
    };
};
export default Icon;
